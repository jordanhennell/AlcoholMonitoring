<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.alcoholmonitoring">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".track_calendar.CalendarActivity"
            android:label="Calendar" />
        <activity
            android:name=".alcohol_details.AlcoholDetailsTrack"
            android:label="DrinkDetail" />
        <activity
            android:name=".view.SettingsActivity"
            android:label="Settings" />
        <activity
            android:name=".view.DrinkingRecordActivity"
            android:label="DrinkingRecord" />
        <activity
            android:name=".view.RecordViewActivity"
            android:label="ViewRecord" />
        <activity
            android:name=".view.WeeklySummaryActivity"
            android:label="ViewRecord" />

        <receiver android:name=".utils.AlarmReceiver">
            <intent-filter>
                <action android:name="project.alcoholmonitoring.action.ACTION_ALCOHOL_ALARM" />
            </intent-filter>
        </receiver>

        <activity android:name=".event.EventActivity"></activity>
    </application>

</manifest>